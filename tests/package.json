{
  "name": "wu-home-tests",
  "private": true,
  "description": "晤 · With You 的体检。跟主程序分开：主程序零依赖，这里的依赖只在体检时用，不会进部署。",
  "scripts": {
    "check": "node run.js",
    "list": "node run.js --list"
  },
  "devDependencies": {
    "eslint": "^9.39.0",
    "globals": "^17.0.0",
    "playwright-core": "1.63.0"
  }
}
